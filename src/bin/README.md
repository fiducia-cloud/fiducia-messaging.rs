# Binaries

Compatibility utilities for inspecting or migrating legacy message envelopes.
The library remains the canonical runtime API; this directory must not grow a
second queue or coordination authority.
